shared_library("LibThreading") {
  output_name = "threading"
  include_dirs = [ "//Libraries" ]
  sources = [
    "BackgroundAction.cpp",
    "Thread.cpp",
  ]
  deps = [
    "//AK",
    "//Libraries/LibCore",
  ]
}
